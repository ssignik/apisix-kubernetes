apiVersion: apisix.apache.org/v2
kind: ApisixPluginConfig
metadata:
  name: security-cors
  namespace: ingress-apisix
spec:
  plugins:
  - name: cors
    enable: true
    config:
      allow_origins: "http://allow.origin.org"